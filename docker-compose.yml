name: inbox-zero-services
services:
  db:
    image: postgres:16
    restart: "no"
    container_name: inbox-zero
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-inboxzero}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-password}
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres}']
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - database-data:/var/lib/postgresql/data/
    ports:
      - ${POSTGRES_PORT:-5432}:5432
    networks:
      - inbox-zero-network
    profiles:
      - local-db
      - all

  redis:
    image: redis:7
    ports:
      - ${REDIS_PORT:-6380}:6379
    volumes:
      - database-data:/data
    networks:
      - inbox-zero-network
    restart: "no"
    profiles:
      - local-redis
      - all

  serverless-redis-http:
    ports:
      - "${REDIS_HTTP_PORT:-8079}:80"
    image: hiett/serverless-redis-http:latest
    env_file:
      - ./apps/web/.env
    environment:
      SRH_MODE: env
      SRH_TOKEN: ${UPSTASH_REDIS_TOKEN}
      SRH_CONNECTION_STRING: "redis://redis:6379" # Using `redis` hostname since they're in the same Docker network.
    networks:
      - inbox-zero-network
    restart: "no"
    profiles:
      - local-redis
      - all

  azure-openai-proxy:
    image: azure-openai-proxy:custom
    container_name: azure-openai-proxy
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    networks:
      - inbox-zero-network
    restart: "no"
    profiles:
      - all

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    image: inbox-zero-custom:latest
    env_file:
      - ./apps/web/.env
    ports:
      - ${WEB_PORT:-3000}:3000
    networks:
      - inbox-zero-network
    environment:
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL:-http://localhost:3000}
      NEXT_PUBLIC_BYPASS_PREMIUM_CHECKS: ${NEXT_PUBLIC_BYPASS_PREMIUM_CHECKS:-true}
      NEXT_PUBLIC_EMAIL_SEND_ENABLED: ${NEXT_PUBLIC_EMAIL_SEND_ENABLED:-true}
      NEXT_PUBLIC_MEETING_BRIEFS_ENABLED: ${NEXT_PUBLIC_MEETING_BRIEFS_ENABLED:-true}
      NEXT_PUBLIC_DIGEST_ENABLED: ${NEXT_PUBLIC_DIGEST_ENABLED:-true}
      NEXT_PUBLIC_INTEGRATIONS_ENABLED: ${NEXT_PUBLIC_INTEGRATIONS_ENABLED:-true}
      NEXT_PUBLIC_CONTACTS_ENABLED: ${NEXT_PUBLIC_CONTACTS_ENABLED:-true}
      # Database and Redis - injected from environment or .env file
      DATABASE_URL: ${DATABASE_URL}
      DIRECT_URL: ${DIRECT_URL}
      UPSTASH_REDIS_URL: ${UPSTASH_REDIS_URL}
      UPSTASH_REDIS_TOKEN: ${UPSTASH_REDIS_TOKEN}
      INTERNAL_API_URL: ${INTERNAL_API_URL:-http://web:3000}
    restart: "no"

  cron:
    image: alpine:latest
    command: >
      sh -c "
        apk add --no-cache curl &&
        WATCH_INTERVAL=21600 &&
        MEETING_BRIEFS_INTERVAL=900 &&
        LAST_WATCH=0 &&
        while true; do
          NOW=$$(date +%s)
          
          # Meeting briefs: every 15 minutes
          echo \"[cron] Processing meeting briefs...\"
          curl -s -X GET 'http://web:3000/api/meeting-briefs' -H \"Authorization: Bearer $${CRON_SECRET}\" || echo \"[cron] Warning: meeting-briefs request failed\"
          
          # Email watch renewal: every 6 hours
          if [ $$((NOW - LAST_WATCH)) -ge $$WATCH_INTERVAL ]; then
            echo \"[cron] Renewing email watches...\"
            curl -s -X GET 'http://web:3000/api/watch/all' -H \"Authorization: Bearer $${CRON_SECRET}\" || echo \"[cron] Warning: watch request failed\"
            LAST_WATCH=$$NOW
          fi
          
          echo \"[cron] Sleeping for 15 minutes...\"
          sleep $$MEETING_BRIEFS_INTERVAL
        done
      "
    env_file:
      - ./apps/web/.env
    depends_on:
      - web
    networks:
      - inbox-zero-network
    restart: "no"

volumes:
  database-data:

networks:
  inbox-zero-network:
